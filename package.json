{
  "name": "abstracted-admin",
  "version": "0.0.1",
  "description": "Helpful abstraction on the firebase admin API",
  "license": "MIT",
  "repository": "https://github.com/forest-fire/fireadmin",
  "author": "Ken Snyder <ken@ken.net>",
  "keywords": [
    "firebase",
    "typescript",
    "nodejs",
    "utility",
    "admin"
  ],
  "files": [
    "lib"
  ],
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "scripts": {
    "clean": "rimraf lib",
    "lint": "tslint --force --format verbose \"src/**/*.ts\"",
    "build": "npm run clean && npm run lint && echo Using TypeScript && tsc --version && tsc --pretty",
    "test": "npm run build && mocha --compilers ts:ts-node/register --recursive 'test/**/*-spec.ts'",
    "coverage": "nyc --include='src/**/*.ts' --reporter=text --reporter=html --reporter=lcov mocha --compilers ts:ts-node/register --recursive 'test/**/*.test.ts'",
    "watch": "npm run build -- --watch",
    "watch:test": "npm run test -- --watch"
  },
  "dependencies": {
    "chance": "^1.0.10",
    "common-types": "^1.2.1",
    "faker": "^4.1.0",
    "firebase-admin": "5.1.0",
    "moment": "^2.18.1",
    "typed-conversions": "^0.6.7"
  },
  "devDependencies": {
    "@types/chance": "^0.7.34",
    "@types/faker": "^4.1.0",
    "@types/chai": "^3.0.0",
    "@types/js-yaml": "^3.9.0",
    "@types/lodash": "^4.14.71",
    "@types/mocha": "^2.0.0",
    "@types/node": "^6.0.0",
    "chai": "^3.0.0",
    "coveralls": "^2.0.0",
    "firemock": "^0.10.5",
    "js-yaml": "^3.9.1",
    "lodash": "^4.17.4",
    "mocha": "^3.0.0",
    "nyc": "^10.0.0",
    "rimraf": "^2.0.0",
    "test-console": "^1.0.0",
    "ts-node": "^3.0.0",
    "tslint": "^5.6.0",
    "typescript": "^2.4.2"
  },
  "engines": {
    "node": ">=4.0.0"
  },
  "nyc": {
    "include": [
      "src/**/*.ts"
    ],
    "exclude": [
      "lib"
    ],
    "extension": [
      ".ts"
    ],
    "require": [
      "ts-node/register"
    ],
    "reporter": [],
    "all": true
  }
}
